# Desarrollo local (vite):
VITE_API_URL=http://localhost:8000

# Deploy con Docker (nginx proxy /api → backend):
# VITE_API_URL=/api
#
# Front y back en dominios separados:
# VITE_API_URL=https://api.tudominio.com
